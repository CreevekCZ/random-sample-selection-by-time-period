name: csv_filter_cli
description: >
  A Dart script for selecting a random 15% sample of data from each month's export 
  files generated by Facepager.
version: 1.0.0

environment:
  sdk: ^3.1.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
